   git init
  git status
   git add file_name
   git status
   git commit -m "Added testing file"
   rm file_name
   ls
   git status
   git restore file_name
   git clone url_of_repo
   git push origin master
The commands that we have learned
